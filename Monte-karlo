from math import sin

x_min = float(input("Введите значение минимального x:"))
x_max = float(input("Введите значение максимального x:"))
number_point = int(input("Введите число точек"))

def function(x):
    y = 1 + x + sin(2*x)
    return y

xlen = x_max - x_min

S =


